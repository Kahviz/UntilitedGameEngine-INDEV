Code style to Boron

Use this format in:
	1. in getters "getFoo()" and "getBoo()"
	2. in functions "Init()" and "Destroy()"

Type member variables "m_variable"
Type parametre variables "p_variable"
Type global variables "g_variable" 

Use camelCase